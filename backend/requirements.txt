fastapi>=0.115
uvicorn[standard]>=0.30
psycopg[binary]>=3.2
openai>=1.30
requests>=2.31
boto3>=1.34
python-dotenv>=1.2
yt-dlp>=2026.7.4
# PO token provider plugin for YouTube bot-mitigation (sidecar at POT_PROVIDER_URL)
# See https://github.com/Brainicism/bgutil-ytdlp-pot-provider
bgutil-ytdlp-pot-provider>=1.2.0
google-api-python-client>=2.0
google-auth>=2.0

# API / ORM / migrations
sqlalchemy>=2.0
alembic>=1.13
pydantic[email]>=2.0
argon2-cffi>=23.1
cryptography>=50.0

# Local-model deployments (TRANSCRIPTION_PROVIDER=local).
# whisper.cpp bindings; prebuilt manylinux wheels (no compiler needed).
# Models are downloaded on first use to /root/.local/share/pywhispercpp/models
# — persist that path with a volume (see docker-compose.yml).
pywhispercpp>=1.3
